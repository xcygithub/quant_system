# 2026-04-03 工作记录

## 量化交易系统 - 今日修改汇总

### 第六次修改（下午）- K线图优化

**新增功能**：
1. **K线周期切换**：支持日K、周K、月K、年K
2. **移动平均线显示**：可选择显示 MA5、MA10、MA20、MA60、MA120
3. **K线图美化**：
   - 中国股市配色（红涨绿跌）
   - 添加hover信息
   - 添加网格线
   - 更美观的布局

**修改文件**：`web/app.py`

**关键实现**：
- `convert_to_period()`: 将日K转换为周/月/年K，使用pandas resample
- `calculate_ma()`: 计算移动平均线
- `plot_candlestick_chart()`: 绘制精美的K线图
- `plot_volume_chart()`: 绘制成交量图

**注意**：pandas 新版本中 'M' 改为 'ME'，'Y' 改为 'YE'

---

## 数据库相关

**SQLite 表结构** (`quant_data.db`)：
- `daily_kline`: symbol, date, open, high, low, close, volume, amount, turnover, amplitude, pct_change, change_amount

**修复问题**：
- `_save_kline_to_db()` 列名不一致问题（change vs change_amount）
- 现在可以正确缓存数据到SQLite

---

## 回测系统增强

**交易明细功能**（第五次修改）：
- 买入/卖出记录（买入数量=100整数倍）
- 持有中状态（未平仓持仓）
- 收益率和净收益率
- 持有时间（自然日+交易日）

**收益率计算**（第四次修改）：
- 统一按10%仓位计算
- 绩效分析与交易明细一致

---

## 数据源优先级

1. AkshareDataSource（重试3次）
2. BaostockDataSource
3. EastmoneyDataSource
4. CSVDataSource
5. SimulatedDataSource