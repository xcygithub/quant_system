# 2026-04-22 工作记录

## 多因子回测因子值展示功能开发完成

### 修改内容
1. **multi_stock_backtest.py**:
   - `TradeDetail` 新增 `stock_name`、`factor_values`、`factor_percentiles`、`composite_score`、`rank`、`total_candidates` 字段
   - `_buy()` 新增 `stock_name` 参数
   - `get_trade_details_df()` 股票显示格式改为 `代码 - 中文名`，新增因子相关列

2. **multi_factor_backtest.py**:
   - 新增 `RebalanceSnapshot` 数据类记录调仓快照
   - 新增三个缓存字典：`_factor_panel_cache`、`_factor_percentiles_cache`、`_composite_scores_cache`
   - 新增 `stock_names` 映射
   - 重写 `_rebalance()` 记录调仓快照和因子信息
   - 新增 `_buy_multi_factor()` 调用父类买入并补充因子信息
   - `_generate_factor_signals()` 中缓存因子面板、百分位、综合得分
   - `run()` 返回结果新增 `rebalance_snapshots` 和 `factor_names`
   - `run_multi_factor_backtest()` 新增 `stock_names` 参数

3. **factor_backtest_page.py**:
   - `_execute_backtest()` 构建股票名称映射并传入回测引擎
   - 交易明细新增「显示因子得分列」复选框 + 因子雷达图展开详情
   - 新增「调仓记录」标签页：调仓概览 + 单次调仓详情 + 候选股票因子面板表格 + 因子得分热力图
   - 股票显示格式改为 `代码 - 中文名`

### Bug 修复
- **run() 清空缓存问题**：`run()` 开头重置了 `_factor_panel_cache` 等缓存，但父类 `run()` 不会重新调用 `set_data()`，导致 `_rebalance()` 中缓存为空。修复：只清空 `rebalance_snapshots`，不清空因子缓存。

### 测试
- test_factor_display.py 4 项测试全部通过
